- @body_id = 'cart'
%h1= t("shopping_cart")
- form_for(@order, :url => object_url, :html => { :method => :put}) do |order_form|
  = render :partial => 'form', :locals => {:order_form => order_form}
%p= link_to t("continue_shopping"), products_path
