- if !@product.taxons.blank?
  #taxon-crumbs
    %h3= t('look_for_similar_items')
    - hook :product_taxons do
      %div
        %ul#similar_items_by_taxon
          - @product.taxons.each do |taxon|
            %li= link_to taxon.name, "/t/#{taxon.permalink}"